# ⏰ Digital Clock

A simple, responsive digital clock built with HTML, CSS, and JavaScript. It displays real-time hours, minutes, seconds, and AM/PM using live DOM updates.

## 🚀 Features

* Real-time clock (updates every second)
* 12-hour format with AM/PM
* Clean UI with frosted-glass effect
* Fullscreen background image
* Lightweight and dependency-free

## 🛠️ Tech Used

• HTML 
• CSS 
• JavaScript (ES6+)

## 💡 Highlights

* Uses `Date()` for time values
* Formats time with leading zeros
* Updates UI with `setInterval()`
* Styled with Flexbox and backdrop blur

